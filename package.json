{
  "name": "iupfa-page",
  "version": "1.0.0",
  "private": true,
  "description": "Portal web de IUPFA: sitio estático (HTML/CSS/JS) conectado directo a Supabase, sin build.",
  "scripts": {
    "start": "serve . -l 3000"
  },
  "devDependencies": {
    "serve": "^14.2.3"
  }
}
